FROM 547749435 as builder

WORKDIR ./app/point

COPY ./pl/* .

RUN npm config set registry=http://ps/npm-all/
RUN npm config set always-auth=true
RUN npm config set _auth=UExrWlJNNE4xMA==

RUN npm install -g serve

CMD ["serve", "-p", "8080"]